export default {
    content: [],
    theme: {
      extend: {
        fontFamily: {
          custom: ['Geist']
        }
      },
    },
    plugins: [
      require('@tailwindcss/typography'),
    ],
    darkMode: 'class',
  }